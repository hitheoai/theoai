{
  "name": "theoai",
  "version": "0.1.0",
  "private": true,
  "description": "Theo AI — SDK, MCP server, channel adapters, and documentation.",
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces --if-present",
    "clean": "npm run clean --workspaces --if-present"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "typescript": "^5",
    "vitest": "^4.1.1"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
